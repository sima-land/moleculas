import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import {SearchBar} from '../';
import { Primary } from './search-bar.stories.jsx'


<Meta
  title='mobile/SearchBar'
  component={SearchBar}
  parameters={{
    controls: { expanded: true },
    viewport: {
      defaultViewport: 'iphonex',
    },
  }}
/>


# SearchBar
### Компонент поисковой строки.

### Пример использования
```jsx
import SearchBar from '@dev-dep/moleculas/mobile/components/search-bar';

<SearchBar
  title='Example'
  onClick={onClick}
/>
```

### Базовый вид
export const Template = args => <SearchBar {...args} />;

<Canvas>
  <Story story={Primary}/>
</Canvas>

### Свойства компонента
<ArgsTable of={SearchBar} />
