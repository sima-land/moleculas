import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { PersonScreen } from '../';
import {
  NoArbitraryLinkView,
  ArbitraryLinkView,
} from './person-screen.stories.jsx';

<Meta
  title='mobile/PersonScreen'
  component={PersonScreen}
  parameters={{
    controls: { expanded: true },
    viewport: {
      defaultViewport: 'iphone6',
    },
  }}
/>

# PersonScreen
### Компонент экрана персональных данных.
Компонент мобильной версии. Может использоваться без добавления произвольной ссылки.
Варианты отображения представлены в конце документации.

### Пример использования
```jsx
import { PersonScreen } from '@dev-dep/moleculas/mobile/components/person-screen';

<PersonScreen {...PersonScreenProps} />
```

### Вид без добавления произвольной ссылки
<Canvas>
  <Story story={NoArbitraryLinkView} />
</Canvas>

### Вид с добавлением произвольной ссылки
<Canvas>
  <Story story={ArbitraryLinkView} />
</Canvas>

### Свойства компонента
<ArgsTable of={ListItem} />

