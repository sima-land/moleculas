import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import PseudoInput from '../';
import { Primary } from './pseudo-input.stories.jsx'


<Meta
  title='mobile/PseudoInput'
  component={PseudoInput}
/>

# PseudoInput
### Компонент псевдо-инпута.

### Пример использования
```jsx
import PseudoInput from '@dev-dep/moleculas/mobile/components/pseudo-input';

<PseudoInput
  title='Example'
  onClick={onClick}
/>
```

### Базовый вид
export const Template = args => <PseudoInput {...args} />;

<Canvas>
  <Story story={Primary}/>
</Canvas>

### Свойства компонента
<ArgsTable of={PseudoInput} />
