import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import GarlandCalculatorButton from '../index';
import { Primary } from './garland-calculator-button.stories'

<Meta
  title='mobile/GarlandCalculatorButton'
  component={GarlandCalculatorButton}
  argTypes={{
    onClick: {
      action: 'Click event',
    },
  }}
/>

# GarlandCalculatorButton
### Компонент плашки с кнопкой открытия гирлянды.

### Пример использования
```jsx
import GarlandCalculatorButton from '@dev-dep/moleculas/mobile/garland-calculator-button';

<GarlandCalculatorButton
  onClick={() => {}}
/>
```

### Базовый вид
export const Template = args => <GarlandCalculatorButton {...args} />;

<Canvas>
  <Story story={Primary}/>
</Canvas>

### Свойства компонента
<ArgsTable of={GarlandCalculatorButton} />
