import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
// Импортируем чистый компонент без HOC, чтобы корректно отображались PropTypes.
import { RecommendationCarousel } from '../index';
import { Primary } from './recommendation-carousel.stories.jsx'

<Meta
  title='desktop/Recommendation carousel'
  component={RecommendationCarousel}
  argTypes={{
    items: {
      control: {
        type: null,
      },
    },
  }}
/>

# RecommendationCarousel
### Компонент карусели рекомендованных товаров.
Компонент десктопной версии. Имеет бесконечную прокрутку. Обернут в два HOC `withInViewportObserver`
для загрузки элементов карусели и отправки статистики в ОКО.

### Пример использования
```jsx
import RecommendationCarousel from '@dev-dep/moleculas/desktop/components/recommendation-carousel';

<RecommendationCarousel
  title='Рекомендованные товары'
  titleContainer='h3'
  items={items}
  onItemClick={() => undefined}
  onQuickViewClick={() => undefined}
  onInViewport={() => undefined}
  onRequest={() => undefined}
  wrapperClass='custom-class'
/>
```

### Базовый вид
export const Template = args => <RecommendationCarousel {...args} />;

<Canvas>
  <Story story={Primary}/>
</Canvas>

### Свойства компонента
<ArgsTable of={RecommendationCarousel} />

